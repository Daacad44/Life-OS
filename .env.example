# Root env — used by the Prisma CLI (prisma migrate, prisma studio, etc).
# Runtime env for the API itself lives in apps/api/.env — see apps/api/.env.example.

DATABASE_URL="postgresql://user:password@localhost:5432/life_os?schema=public"
